{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 1?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 1;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_001"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 2?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 2;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_002"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 3?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 3;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_003"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 4?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 4;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_004"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 5?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 5;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_005"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 6?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 6;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_006"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 7?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 7;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_007"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 8?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 8;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_008"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 9?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 9;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_009"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 10?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 10;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_010"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 11?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 11;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_011"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 12?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 12;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_012"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 13?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 13;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_013"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 14?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 14;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_014"}
{"database_id": "shop", "question_ru": "Какая разница между максимальной ценой товара и средней ценой товара в категории 15?", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 15;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_015"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 1?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 1 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_016"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 2?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 2 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_017"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 3?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 3 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_018"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 4?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 4 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_019"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 5?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 5 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_020"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 6?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 6 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_021"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 7?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 7 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_022"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 8?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 8 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_023"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 9?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 9 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_024"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 10?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 10 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_025"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 11?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 11 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_026"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 12?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 12 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_027"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 13?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 13 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_028"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 14?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 14 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_029"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе 15?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 15 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_030"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 1, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 1 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_031"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 2, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 2 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_032"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 3, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 3 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_033"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 4, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 4 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_034"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 5, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 5 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_035"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 6, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 6 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_036"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 7, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 7 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_037"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 8, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 8 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_038"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 9, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 9 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_039"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 10, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 10 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_040"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 11, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 11 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_041"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 12, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 12 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_042"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 13, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 13 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_043"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 14, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 14 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_044"}
{"database_id": "support", "question_ru": "Покажи количество тикетов, созданных клиентом 15, которые решались дольше среднего.", "sql": "SELECT COUNT(*) FROM tickets WHERE client_id = 15 AND (julianday(resolved_at) - julianday(created_at)) > (SELECT AVG(julianday(resolved_at) - julianday(created_at)) FROM tickets WHERE resolved_at IS NOT NULL);", "explanation_ru": "Сложный запрос со временем и вложенным селектом для среднего.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "date"], "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_045"}
