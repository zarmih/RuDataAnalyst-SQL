{"database_id": "shop", "question_ru": "Для каждого города покажи количество доставленных заказов и общую сумму этих заказов, отсортировав по убыванию суммы.", "sql": "SELECT c.city, COUNT(o.id) AS order_count, SUM(oi.quantity * oi.unit_price) AS total_sum FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id WHERE o.status = 'delivered' GROUP BY c.city ORDER BY total_sum DESC;", "explanation_ru": "Сложный запрос с двумя JOIN, GROUP BY, агрегацией и сортировкой.", "difficulty": "hard", "concepts": ["select", "join", "where", "group_by", "aggregate", "order_by", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0001"}
{"database_id": "shop", "question_ru": "Какой процент товаров находится в категории 'Электроника'? (в долях от 0 до 1)", "sql": "SELECT CAST(SUM(CASE WHEN c.name = 'Электроника' THEN 1 ELSE 0 END) AS REAL) / COUNT(*) AS fraction FROM products p JOIN categories c ON p.category_id = c.id;", "explanation_ru": "Использование CASE WHEN внутри SUM для расчета пропорции.", "difficulty": "hard", "concepts": ["select", "join", "aggregate", "case_when", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0002"}
{"database_id": "hr", "question_ru": "Найди сотрудников, чья зарплата превышает бюджет их отдела.", "sql": "SELECT e.first_name, e.last_name FROM employees e JOIN departments d ON e.department_id = d.id WHERE e.salary > d.budget;", "explanation_ru": "Сравнение колонки одной таблицы с колонкой другой таблицы.", "difficulty": "hard", "concepts": ["select", "join", "where", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0003"}
{"database_id": "hr", "question_ru": "Покажи проекты, в которых участвуют сотрудники из более чем одного отдела.", "sql": "SELECT p.name FROM projects p JOIN project_assignments pa ON p.id = pa.project_id JOIN employees e ON pa.employee_id = e.id GROUP BY p.id, p.name HAVING COUNT(DISTINCT e.department_id) > 1;", "explanation_ru": "Сложный HAVING с COUNT(DISTINCT) после соединения 3 таблиц.", "difficulty": "hard", "concepts": ["select", "join", "group_by", "having", "aggregate", "distinct", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0004"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 1.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 1;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0005"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 1?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 1 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0006"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 2.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 2;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0007"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 2?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 2 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0008"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 3.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 3;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0009"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 3?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 3 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0010"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 4.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 4;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0011"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 4?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 4 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0012"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 5.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 5;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0013"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 5?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 5 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0014"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 6.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 6;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0015"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 6?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 6 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0016"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 7.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 7;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0017"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 7?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 7 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0018"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 8.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 8;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0019"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 8?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 8 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0020"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 9.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 9;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0021"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 9?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 9 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0022"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 10.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 10;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0023"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 10?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 10 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0024"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 11.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 11;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0025"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 11?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 11 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0026"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 12.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 12;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0027"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 12?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 12 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0028"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 13.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 13;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0029"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 13?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 13 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0030"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 14.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 14;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0031"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 14?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 14 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0032"}
{"database_id": "shop", "question_ru": "Покажи разницу между максимальной ценой товара и средней ценой товара в категории с ID 15.", "sql": "SELECT MAX(price) - AVG(price) AS diff FROM products WHERE category_id = 15;", "explanation_ru": "Арифметика с агрегатными функциями.", "difficulty": "hard", "concepts": ["select", "where", "aggregate", "alias"], "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0033"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 15?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 15 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0034"}
{"database_id": "hr", "question_ru": "Сколько сотрудников без проектов в отделе с ID 7, пожалуйста?", "sql": "SELECT COUNT(*) FROM employees e WHERE e.department_id = 7 AND e.id NOT IN (SELECT employee_id FROM project_assignments);", "explanation_ru": "Комбинация условия и подзапроса NOT IN.", "difficulty": "hard", "concepts": ["select", "where", "subquery", "aggregate", "alias"], "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "split": "challenge", "assumptions": [], "context": "", "id": "challenge_0035"}
