{"id": "hr_val_0004", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Покажи минимальную и максимальную зарплату по каждому отделу.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0023", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. Заранее спасибо!", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0029", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Мне нужно узнать: какие тарифы используют больше одного клиента? (в формате таблицы).", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0011", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Бот, Выведи сколько отменённых заказов было сделано? (в формате таблицы).", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0030", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Коллега, Можешь показать покажи товары из категории «книги» или «продукты».", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0005", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Слушай, Какой клиент потратил больше всего денег? Покажи имя и сумму. Заранее спасибо! - это нужно для отчета.", "sql": "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id GROUP BY c.id, c.name ORDER BY total_spent DESC LIMIT 1;", "explanation_ru": "Соединяем три таблицы, суммируем стоимость по клиенту, берём максимум.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "join", "group_by", "aggregate", "order_by", "limit", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0018", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Эй, Покажи список неактивных агентов (быстро). - это нужно для отчета.", "sql": "SELECT name FROM agents WHERE is_active = 0;", "explanation_ru": "Фильтр агентов с is_active = 0.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where"], "split": "validation", "context": ""}
{"id": "shop_val_0024", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Эй, Сколько отменённых заказов было сделано? Заранее спасибо! - это нужно для отчета.", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0027", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Как выглядит выведи имена клиентов, у которых нет тикетов со статусом closed. как можно скорее.", "sql": "SELECT company_name FROM clients WHERE id NOT IN (SELECT client_id FROM tickets WHERE status = 'closed');", "explanation_ru": "Использование подзапроса NOT IN.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "shop_val_0022", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Ассистент, Требуется отчет: покажи товары из категории «книги» или «продукты». (в формате таблицы).", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "support_val_0020", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Эй, Будь добр, выведи имена клиентов, у которых нет тикетов со статусом closed. - только точные данные.", "sql": "SELECT company_name FROM clients WHERE id NOT IN (SELECT client_id FROM tickets WHERE status = 'closed');", "explanation_ru": "Использование подзапроса NOT IN.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "shop_val_0009", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Слушай, Аналитика: покажи уникальные города клиентов. - только точные данные.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
{"id": "hr_val_0026", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Эй, Аналитика: покажи минимальную и максимальную зарплату по каждому отделу. (в формате таблицы).", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0001", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Покажи сотрудников с зарплатой ниже средней по компании.", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0017", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Коллега, Покажи минимальную и максимальную зарплату по каждому отделу., пожалуйста. - это нужно для отчета.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0001", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Покажи список неактивных агентов.", "sql": "SELECT name FROM agents WHERE is_active = 0;", "explanation_ru": "Фильтр агентов с is_active = 0.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where"], "split": "validation", "context": ""}
{"id": "shop_val_0006", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Требуется отчет: сколько отменённых заказов было сделано? (в формате таблицы).", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0022", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Ассистент, Будь добр, покажи роли сотрудников на активных проектах. - только точные данные.", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0010", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи товары из категории «Книги» или «Продукты». (очень важно) как можно скорее.", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "support_val_0028", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Сколько сообщений со статусом bug? - спасибо. - только точные данные.", "sql": "SELECT COUNT(tm.id) FROM ticket_messages tm JOIN tickets t ON tm.ticket_id = t.id WHERE t.category = 'bug';", "explanation_ru": "Подсчет сообщений в тикетах с категорией bug.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0027", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Покажи роли сотрудников на активных проектах. (очень важно) (в формате таблицы).", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0020", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Бот, Покажи товары из категории «Книги» или «Продукты». (очень важно) - это нужно для отчета.", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0018", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи товары из категории «Книги» или «Продукты». Заранее спасибо!", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "support_val_0021", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Будь добр, сколько сообщений со статусом bug? - это нужно для отчета.", "sql": "SELECT COUNT(tm.id) FROM ticket_messages tm JOIN tickets t ON tm.ticket_id = t.id WHERE t.category = 'bug';", "explanation_ru": "Подсчет сообщений в тикетах с категорией bug.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0012", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Эй, Можешь показать сколько отменённых заказов было сделано? (в формате таблицы).", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0002", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "В каких отделах средняя зарплата превышает 130000?", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0028", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Аналитика: сколько отменённых заказов было сделано?", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0030", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, В каких отделах средняя зарплата превышает 130000? (быстро). - только точные данные.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0005", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0006", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Коллега, Будь добр, найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. (в формате таблицы).", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "hr_val_0009", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, Покажи минимальную и максимальную зарплату по каждому отделу. - только точные данные.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0028", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Бот, Сделай SQL запрос: в каких отделах средняя зарплата превышает 130000? - это нужно для отчета.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0013", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Коллега, Покажи товары из категории «Книги» или «Продукты». (срочно) как можно скорее.", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0017", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Ассистент, Покажи товары из категории «Книги» или «Продукты». - только точные данные.", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0001", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи уникальные города клиентов.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
{"id": "hr_val_0020", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, Интересует покажи сотрудников с зарплатой ниже средней по компании. - это нужно для отчета.", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0003", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Какие тарифы используют больше одного клиента?", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0010", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Мне нужно узнать: найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0004", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Выведи имена клиентов, у которых нет тикетов со статусом closed.", "sql": "SELECT company_name FROM clients WHERE id NOT IN (SELECT client_id FROM tickets WHERE status = 'closed');", "explanation_ru": "Использование подзапроса NOT IN.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "hr_val_0011", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Коллега, Сделай SQL запрос: в каких отделах средняя зарплата превышает 130000? - только точные данные.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0021", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Эй, В каких отделах средняя зарплата превышает 130000? Заранее спасибо! - только точные данные.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0017", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Бот, Мне нужно узнать: найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. (в формате таблицы).", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "hr_val_0013", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Бот, Интересует покажи сотрудников с зарплатой ниже средней по компании. - только точные данные.", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0019", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента., пожалуйста. - только точные данные.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "hr_val_0015", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, Аналитика: в каких отделах средняя зарплата превышает 130000? - это нужно для отчета.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0008", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента., пожалуйста. как можно скорее.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "shop_val_0016", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Бот, Можешь показать сколько отменённых заказов было сделано? - только точные данные.", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0025", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Коллега, Аналитика: сколько отменённых заказов было сделано?", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0029", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Ассистент, Можешь показать покажи минимальную и максимальную зарплату по каждому отделу. как можно скорее.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0012", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Можешь показать в каких отделах средняя зарплата превышает 130000?", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0024", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Бот, Подскажи, найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. как можно скорее.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0030", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Какие тарифы используют больше одного клиента? (срочно) - это нужно для отчета.", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0006", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Эй, Как выглядит покажи сотрудников с зарплатой ниже средней по компании.", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0018", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Эй, Не мог бы ты сказать, в каких отделах средняя зарплата превышает 130000? - только точные данные.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0014", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Коллега, Сделай SQL запрос: покажи минимальную и максимальную зарплату по каждому отделу. - это нужно для отчета.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0010", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Аналитика: в каких отделах средняя зарплата превышает 130000?", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0025", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Коллега, Не мог бы ты сказать, выведи имена клиентов, у которых нет тикетов со статусом closed. как можно скорее.", "sql": "SELECT company_name FROM clients WHERE id NOT IN (SELECT client_id FROM tickets WHERE status = 'closed');", "explanation_ru": "Использование подзапроса NOT IN.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "shop_val_0029", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Слушай, Какой клиент потратил больше всего денег? Покажи имя и сумму., пожалуйста. - только точные данные.", "sql": "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id GROUP BY c.id, c.name ORDER BY total_spent DESC LIMIT 1;", "explanation_ru": "Соединяем три таблицы, суммируем стоимость по клиенту, берём максимум.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "join", "group_by", "aggregate", "order_by", "limit", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0023", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Ассистент, Покажи роли сотрудников на активных проектах. (срочно) - это нужно для отчета.", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0023", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Будь добр, покажи товары из категории «книги» или «продукты».", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0003", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи товары из категории «Книги» или «Продукты».", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "support_val_0011", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Можешь показать сколько сообщений со статусом bug? как можно скорее.", "sql": "SELECT COUNT(tm.id) FROM ticket_messages tm JOIN tickets t ON tm.ticket_id = t.id WHERE t.category = 'bug';", "explanation_ru": "Подсчет сообщений в тикетах с категорией bug.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0016", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Выведи найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. (в формате таблицы).", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "shop_val_0027", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Слушай, Аналитика: покажи уникальные города клиентов. как можно скорее.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
{"id": "hr_val_0008", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Бот, Сделай SQL запрос: покажи минимальную и максимальную зарплату по каждому отделу. (в формате таблицы).", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0013", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Слушай, Аналитика: найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. - это нужно для отчета.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0026", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Бот, Какие тарифы используют больше одного клиента? Заранее спасибо! - это нужно для отчета.", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0003", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Покажи роли сотрудников на активных проектах.", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0022", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Эй, Какие тарифы используют больше одного клиента, пожалуйста? - это нужно для отчета.", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0021", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Ассистент, Какой клиент потратил больше всего денег? Покажи имя и сумму. - спасибо. как можно скорее.", "sql": "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id GROUP BY c.id, c.name ORDER BY total_spent DESC LIMIT 1;", "explanation_ru": "Соединяем три таблицы, суммируем стоимость по клиенту, берём максимум.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "join", "group_by", "aggregate", "order_by", "limit", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0005", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Бот, Покажи минимальную и максимальную зарплату по каждому отделу. - только точные данные.", "sql": "SELECT d.name, MIN(e.salary) AS min_salary, MAX(e.salary) AS max_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name;", "explanation_ru": "Группируем по отделу, находим MIN и MAX зарплаты.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0026", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Эй, Требуется отчет: сколько отменённых заказов было сделано? - только точные данные.", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0008", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи товары из категории «Книги» или «Продукты». - спасибо. - это нужно для отчета.", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0007", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Покажи товары из категории «Книги» или «Продукты»., пожалуйста. (в формате таблицы).", "sql": "SELECT p.name, p.price FROM products p JOIN categories c ON p.category_id = c.id WHERE c.name IN ('Книги', 'Продукты');", "explanation_ru": "Фильтруем товары по принадлежности к категориям через IN.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "join", "where", "in"], "split": "validation", "context": ""}
{"id": "shop_val_0019", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Бот, Интересует покажи уникальные города клиентов.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
{"id": "hr_val_0019", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, Покажи сотрудников с зарплатой ниже средней по компании (быстро). - это нужно для отчета.", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0004", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Сколько отменённых заказов было сделано?", "sql": "SELECT COUNT(*) FROM orders WHERE status = 'cancelled';", "explanation_ru": "Считаем заказы со статусом cancelled.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0014", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Сделай SQL запрос: покажи уникальные города клиентов. как можно скорее.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
{"id": "support_val_0007", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Какие тарифы используют больше одного клиента, пожалуйста? как можно скорее.", "sql": "SELECT plan FROM clients GROUP BY plan HAVING COUNT(id) > 1;", "explanation_ru": "Группировка по плану с условием HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "group_by", "having", "aggregate"], "split": "validation", "context": ""}
{"id": "shop_val_0002", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Какой клиент потратил больше всего денег? Покажи имя и сумму.", "sql": "SELECT c.name, SUM(oi.quantity * oi.unit_price) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN order_items oi ON o.id = oi.order_id GROUP BY c.id, c.name ORDER BY total_spent DESC LIMIT 1;", "explanation_ru": "Соединяем три таблицы, суммируем стоимость по клиенту, берём максимум.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "join", "group_by", "aggregate", "order_by", "limit", "alias"], "split": "validation", "context": ""}
{"id": "hr_val_0025", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Коллега, Мне нужно узнать: покажи сотрудников с зарплатой ниже средней по компании. (в формате таблицы).", "sql": "SELECT first_name, last_name, salary FROM employees WHERE salary < (SELECT AVG(salary) FROM employees);", "explanation_ru": "Подзапрос вычисляет среднюю зарплату, внешний запрос фильтрует.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "where", "subquery", "aggregate"], "split": "validation", "context": ""}
{"id": "hr_val_0007", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, Мне нужно узнать: покажи роли сотрудников на активных проектах. - это нужно для отчета.", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0009", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Слушай, Покажи список неактивных агентов. как можно скорее.", "sql": "SELECT name FROM agents WHERE is_active = 0;", "explanation_ru": "Фильтр агентов с is_active = 0.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "where"], "split": "validation", "context": ""}
{"id": "hr_val_0024", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Слушай, В каких отделах средняя зарплата превышает 130000? (срочно) как можно скорее.", "sql": "SELECT d.name, AVG(e.salary) AS avg_salary FROM departments d JOIN employees e ON d.id = e.department_id GROUP BY d.name HAVING AVG(e.salary) > 130000;", "explanation_ru": "Группируем по отделу, считаем среднюю зарплату, фильтруем HAVING.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "group_by", "having", "aggregate", "alias"], "split": "validation", "context": ""}
{"id": "support_val_0014", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Ассистент, Будь добр, сколько сообщений со статусом bug? как можно скорее.", "sql": "SELECT COUNT(tm.id) FROM ticket_messages tm JOIN tickets t ON tm.ticket_id = t.id WHERE t.category = 'bug';", "explanation_ru": "Подсчет сообщений в тикетах с категорией bug.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0015", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Эй, Аналитика: найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента. - только точные данные.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "support_val_0002", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Сколько сообщений со статусом bug?", "sql": "SELECT COUNT(tm.id) FROM ticket_messages tm JOIN tickets t ON tm.ticket_id = t.id WHERE t.category = 'bug';", "explanation_ru": "Подсчет сообщений в тикетах с категорией bug.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "aggregate"], "split": "validation", "context": ""}
{"id": "support_val_0012", "database_id": "support", "schema_sql": "CREATE TABLE clients (\n    id INTEGER PRIMARY KEY,\n    company_name TEXT NOT NULL,\n    plan TEXT NOT NULL CHECK(plan IN ('free','basic','premium','enterprise')),\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE agents (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    team TEXT NOT NULL CHECK(team IN ('L1','L2','L3')),\n    is_active INTEGER NOT NULL DEFAULT 1\n);\n\nCREATE TABLE tickets (\n    id INTEGER PRIMARY KEY,\n    client_id INTEGER NOT NULL REFERENCES clients(id),\n    agent_id INTEGER REFERENCES agents(id),\n    subject TEXT NOT NULL,\n    priority TEXT NOT NULL CHECK(priority IN ('low','medium','high','critical')),\n    status TEXT NOT NULL CHECK(status IN ('open','in_progress','waiting','resolved','closed')),\n    category TEXT NOT NULL CHECK(category IN ('bug','feature_request','question','incident')),\n    created_at TEXT NOT NULL,\n    resolved_at TEXT,\n    satisfaction_score INTEGER CHECK(satisfaction_score BETWEEN 1 AND 5)\n);\n\nCREATE TABLE ticket_messages (\n    id INTEGER PRIMARY KEY,\n    ticket_id INTEGER NOT NULL REFERENCES tickets(id),\n    sender_type TEXT NOT NULL CHECK(sender_type IN ('client','agent','system')),\n    message TEXT NOT NULL,\n    created_at TEXT NOT NULL\n);", "question_ru": "Найди тикеты, в которых есть сообщения от системы, но нет сообщений от агента (быстро). - это нужно для отчета.", "sql": "SELECT t.subject FROM tickets t WHERE t.id IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'system') AND t.id NOT IN (SELECT ticket_id FROM ticket_messages WHERE sender_type = 'agent');", "explanation_ru": "Пересечение условий с подзапросами IN и NOT IN.", "assumptions": [], "difficulty": "hard", "concepts": ["select", "where", "subquery"], "split": "validation", "context": ""}
{"id": "hr_val_0016", "database_id": "hr", "schema_sql": "CREATE TABLE departments (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    budget REAL\n);\n\nCREATE TABLE employees (\n    id INTEGER PRIMARY KEY,\n    first_name TEXT NOT NULL,\n    last_name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    position TEXT NOT NULL,\n    salary REAL NOT NULL,\n    hire_date TEXT NOT NULL,\n    manager_id INTEGER REFERENCES employees(id)\n);\n\nCREATE TABLE projects (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    department_id INTEGER REFERENCES departments(id),\n    start_date TEXT NOT NULL,\n    end_date TEXT,\n    status TEXT NOT NULL CHECK(status IN ('planning','active','completed','on_hold'))\n);\n\nCREATE TABLE project_assignments (\n    id INTEGER PRIMARY KEY,\n    project_id INTEGER NOT NULL REFERENCES projects(id),\n    employee_id INTEGER NOT NULL REFERENCES employees(id),\n    role TEXT NOT NULL,\n    hours_allocated INTEGER NOT NULL DEFAULT 0\n);", "question_ru": "Ассистент, Не мог бы ты сказать, покажи роли сотрудников на активных проектах.", "sql": "SELECT e.first_name, e.last_name, p.name AS project_name, pa.role FROM project_assignments pa JOIN employees e ON pa.employee_id = e.id JOIN projects p ON pa.project_id = p.id WHERE p.status = 'active';", "explanation_ru": "Соединяем три таблицы и фильтруем по активным проектам.", "assumptions": [], "difficulty": "medium", "concepts": ["select", "join", "where", "alias"], "split": "validation", "context": ""}
{"id": "shop_val_0015", "database_id": "shop", "schema_sql": "CREATE TABLE customers (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    email TEXT UNIQUE,\n    city TEXT,\n    registered_at TEXT NOT NULL\n);\n\nCREATE TABLE categories (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE\n);\n\nCREATE TABLE products (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL,\n    category_id INTEGER NOT NULL REFERENCES categories(id),\n    price REAL NOT NULL CHECK(price > 0),\n    in_stock INTEGER NOT NULL DEFAULT 0\n);\n\nCREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    customer_id INTEGER NOT NULL REFERENCES customers(id),\n    created_at TEXT NOT NULL,\n    status TEXT NOT NULL CHECK(status IN ('new','processing','shipped','delivered','cancelled'))\n);\n\nCREATE TABLE order_items (\n    id INTEGER PRIMARY KEY,\n    order_id INTEGER NOT NULL REFERENCES orders(id),\n    product_id INTEGER NOT NULL REFERENCES products(id),\n    quantity INTEGER NOT NULL CHECK(quantity > 0),\n    unit_price REAL NOT NULL\n);", "question_ru": "Коллега, Покажи уникальные города клиентов (быстро). - только точные данные.", "sql": "SELECT DISTINCT city FROM customers;", "explanation_ru": "Выбираем уникальные значения города из таблицы клиентов.", "assumptions": [], "difficulty": "easy", "concepts": ["select", "distinct"], "split": "validation", "context": ""}
